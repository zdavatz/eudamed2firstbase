log/
logs/
db/*.db
target/
settings.json
p8/
*.p8
eudamed_json/processed/
eudamed_json/detail/
eudamed_json/basic/
swissdamed_json/
firstbase_json/

# config.toml contains secrets (Gmail credentials, GLN).
# Use config.sample.toml as the template — never commit config.toml.
config.toml
*.p12

# Local Claude Code resume helper
resume
resume_icons/

# macOS Finder metadata
.DS_Store

# Customer SRN + GTIN worklists (synced from Google Sheet) — do not commit
srns_sheet.txt
gtins_sheet.txt
run_*.sh
